-- Initialize the database with any required extensions or initial data
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- Create any initial database schema if needed
-- (Alembic will handle most schema creation)